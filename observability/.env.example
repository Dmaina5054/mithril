# /home/dm/Projects/learn/golang/mithril/observability/.env
# Copy to .env and fill in real values. Never commit .env — see .gitignore.

GRAFANA_PASSWORD=<strong-password-here>
GRAFANA_SECRET_KEY=<random-32-char-string>  # openssl rand -hex 16
IPROYAL_API_TOKEN=<your-iproyal-api-token>  # used by proxy, referenced here for completeness
