// Package config loads routing.yaml and profiles.yaml. Runtime config on
// Minas Tirith lives at /etc/socks5-proxy/ — this package only implements
// the loader. Wired up starting Session B Phase 3.
package config
